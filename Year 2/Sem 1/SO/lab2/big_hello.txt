#include <stdio.h>
#include <string.h>

int main()
{
	const char *str = "Hello World with write\n";
	size_t len = strlen(str);
	write(1, str, len);
	return 0;
} // 1
#include <stdio.h>
#include <string.h>

int main()
{
	const char *str = "Hello World with write\n";
	size_t len = strlen(str);
	write(1, str, len);
	return 0;
} // 2
#include <stdio.h>
#include <string.h>

int main()
{
	const char *str = "Hello World with write\n";
	size_t len = strlen(str);
	write(1, str, len);
	return 0;
} // 3
#include <stdio.h>
#include <string.h>

int main()
{
	const char *str = "Hello World with write\n";
	size_t len = strlen(str);
	write(1, str, len);
	return 0;
} // 4
#include <stdio.h>
#include <string.h>

int main()
{
	const char *str = "Hello World with write\n";
	size_t len = strlen(str);
	write(1, str, len);
	return 0;
} // 5
#include <stdio.h>
#include <string.h>

int main()
{
	const char *str = "Hello World with write\n";
	size_t len = strlen(str);
	write(1, str, len);
	return 0;
} // 6
#include <stdio.h>
#include <string.h>

int main()
{
	const char *str = "Hello World with write\n";
	size_t len = strlen(str);
	write(1, str, len);
	return 0;
} // 7